
# ── Web Framework ──
fastapi>=0.111.0            # Async REST API + WebSocket framework
uvicorn[standard]>=0.29.0   # ASGI server to run FastAPI
gunicorn>=22.0.0             # Production process manager with Uvicorn workers
python-multipart>=0.0.9     # Required by FastAPI for form/file uploads
pydantic-settings>=2.0.0    # Typed settings from .env files

# ── Auth & Security ──
bcrypt>=4.0.0                # Password hashing (used directly, not via passlib)
PyJWT>=2.8.0                # JWT Token generation
email-validator>=2.1.0      # Pydantic email validation
aiosmtplib>=3.0.1           # Async SMTP email sending

# ── Databases ──
neo4j>=5.19.0               # Neo4j async driver — temporal knowledge graph
qdrant-client>=1.9.0        # Qdrant vector store client — similarity search

# ── LLM & AI ──
langchain>=0.2.0            # LLM orchestration framework
langgraph>=0.0.30           # Stateful multi-step agent workflows
httpx>=0.27.0               # Async HTTP client — Ollama REST API calls

# ── LLM Providers (optional cloud fallback) ──
openai>=1.0.0               # OpenAI API — GPT fallback when Ollama is down
google-generativeai>=0.5.0  # Google Gemini API — secondary cloud fallback

# ── Code Analysis ──
gitpython>=3.1.43           # Git repo access — walk commits, diffs, branches
tree-sitter>=0.22.0         # AST parser — extract functions, classes, imports

# ── Utilities ──
python-dotenv>=1.0.1        # Load .env file into environment variables
eval-type-backport>=0.2.0   # Python 3.9 compat for X | Y type syntax in Pydantic

# ── Testing ──
pytest>=8.0.0               # Testing framework
pytest-asyncio>=0.23.0      # Asyncio support for pytest
